GIT

git add --all
git commit -m "Имя коммита"
git push

git status //покажет изменённые файлы

git checkout имя-файла //откатит один файл
git reset HEAD --hard  //откатит до последнего коммита




1. Заве
